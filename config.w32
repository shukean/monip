// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ipip", "for ipip support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ipip", "enable ipip support", "no");

if (PHP_IPIP != "no") {
	EXTENSION("ipip", "ipip.c");
}

